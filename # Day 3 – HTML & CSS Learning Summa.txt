# Day 3 – HTML & CSS Learning Summary

Today we created a **static website** project named **Car Hubs**. In this project, we used different **semantic HTML elements** to structure the webpage properly. Semantic elements help make the website organized, readable, and SEO-friendly.

## Semantic Elements Used

* `<header>` → Used for the top section of the website.
* `<nav>` → Used for the navigation bar and menu links.
* `<section>` → Used to create different sections of the webpage.
* `<main>` → Used for the main content area.
* `<footer>` → Used for the footer section.

We also created a **navigation bar (Navbar)** to move between different sections of the website like Home, About, Cars, and Contact.

---

# Using External Header File

Today we learned how to use a separate HTML file for the header and load it dynamically using JavaScript.

We created:

* `header.html` → Contains navbar/header code.
* `index.html` → Main webpage file.

We used the following code to load the header file dynamically:

```javascript
fetch('header.html')
    .then(response => response.text())
    .then(data => {
        const headerContainer = document.querySelector('.navbar');

        if (headerContainer) {
            headerContainer.innerHTML = data;
        }
    })
    .catch(error => console.error('Error loading header:', error));
```

We also used:

```html
<div class="navbar"></div>

<section id="home" class="slider-container">
```

This code helps us insert the `header.html` file into the main webpage automatically. It makes the project cleaner and reusable because we can update the header in one file only.

---

# HTML Code Example

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Car Hubs</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>

    <!-- Navbar Container -->
    <div class="navbar"></div>

    <!-- Home Section -->
    <section id="home" class="container">
        <div class="left-box">
            <h1>Welcome to Car Hubs</h1>
            <p>Best cars collection website.</p>
        </div>

        <div class="center-box">
            <h2>Center Content</h2>
        </div>

        <div class="right-box">
            <h2>Right Content</h2>
        </div>
    </section>

    <script>
        fetch('header.html')
            .then(response => response.text())
            .then(data => {
                const headerContainer = document.querySelector('.navbar');

                if (headerContainer) {
                    headerContainer.innerHTML = data;
                }
            })
            .catch(error => console.error('Error loading header:', error));
    </script>

</body>
</html>
```

---

# CSS Code Example

```css
*{
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body{
    font-family: Arial, sans-serif;
}

/* Container */
.container{
    display: flex;
    justify-content: space-between;
    align-items: center;

    width: 90%;
    margin: auto;
    padding: 40px;
}

/* Left Box */
.left-box{
    width: 30%;
    background: lightblue;
    padding: 20px;
    text-align: center;
}

/* Center Box */
.center-box{
    width: 30%;
    background: lightgreen;
    padding: 20px;
    text-align: center;
}

/* Right Box */
.right-box{
    width: 30%;
    background: lightcoral;
    padding: 20px;
    text-align: center;
}
```

---
# Common CSS3 Properties and Their Uses

| CSS3 Property               | Use                                                   |
| --------------------------- | ----------------------------------------------------- |
| `color`                     | Changes text color.                                   |
| `background-color`          | Sets background color.                                |
| `background-image`          | Adds an image as background.                          |
| `background-size`           | Controls background image size.                       |
| `background-position`       | Sets background image position.                       |
| `opacity`                   | Controls transparency level.                          |
| `width`                     | Sets width of an element.                             |
| `height`                    | Sets height of an element.                            |
| `max-width`                 | Sets maximum width.                                   |
| `min-width`                 | Sets minimum width.                                   |
| `margin`                    | Adds outer spacing.                                   |
| `padding`                   | Adds inner spacing.                                   |
| `border`                    | Adds border around element.                           |
| `border-radius`             | Creates rounded corners.                              |
| `box-shadow`                | Adds shadow around element.                           |
| `text-shadow`               | Adds shadow to text.                                  |
| `font-size`                 | Changes text size.                                    |
| `font-family`               | Changes font style.                                   |
| `font-weight`               | Makes text bold/light.                                |
| `font-style`                | Makes text italic.                                    |
| `line-height`               | Sets spacing between lines.                           |
| `letter-spacing`            | Controls spacing between letters.                     |
| `text-align`                | Aligns text left, center, right.                      |
| `text-decoration`           | Adds underline, overline, etc.                        |
| `text-transform`            | Converts text to uppercase/lowercase.                 |
| `display`                   | Defines display type (`block`, `flex`, `grid`).       |
| `visibility`                | Shows or hides element.                               |
| `overflow`                  | Controls extra content visibility.                    |
| `position`                  | Sets element position (`relative`, `absolute`, etc.). |
| `top`                       | Moves element from top.                               |
| `bottom`                    | Moves element from bottom.                            |
| `left`                      | Moves element from left.                              |
| `right`                     | Moves element from right.                             |
| `z-index`                   | Controls stack order of elements.                     |
| `float`                     | Places element left or right.                         |
| `clear`                     | Clears floated elements.                              |
| `cursor`                    | Changes mouse cursor style.                           |
| `list-style`                | Styles list bullets/numbers.                          |
| `outline`                   | Adds outline outside border.                          |
| `box-sizing`                | Controls width/height calculation.                    |
| `resize`                    | Allows resizing of element.                           |
| `filter`                    | Applies effects like blur, brightness.                |
| `transform`                 | Rotates, scales, or moves elements.                   |
| `transition`                | Creates smooth animation effects.                     |
| `animation`                 | Adds animations.                                      |
| `@keyframes`                | Defines animation steps.                              |
| `flex-direction`            | Sets flex item direction.                             |
| `justify-content`           | Aligns flex items horizontally.                       |
| `align-items`               | Aligns flex items vertically.                         |
| `flex-wrap`                 | Wraps flex items into multiple lines.                 |
| `gap`                       | Adds space between flex/grid items.                   |
| `grid-template-columns`     | Defines grid columns.                                 |
| `grid-template-rows`        | Defines grid rows.                                    |
| `grid-gap`                  | Adds spacing between grid items.                      |
| `object-fit`                | Controls image/video fitting.                         |
| `clip-path`                 | Clips element into shapes.                            |
| `media queries`             | Makes website responsive.                             |
| `transition-delay`          | Delays animation start.                               |
| `transition-duration`       | Sets transition speed.                                |
| `animation-duration`        | Sets animation time.                                  |
| `animation-iteration-count` | Repeats animation multiple times.                     |
| `scroll-behavior`           | Enables smooth scrolling.                             |
| `user-select`               | Prevents text selection.                              |
| `pointer-events`            | Controls mouse interactions.                          |
| `white-space`               | Controls text wrapping.                               |
| `word-spacing`              | Adds spacing between words.                           |
| `column-count`              | Splits text into columns.                             |
| `columns`                   | Creates multiple columns.                             |
| `caret-color`               | Changes cursor color in input fields.                 |
| `accent-color`              | Changes checkbox/radio button color.                  |
| `backdrop-filter`           | Applies blur behind element.                          |
| `aspect-ratio`              | Sets width-height ratio.                              |
| `scroll-snap-type`          | Creates scroll snapping effect.                       |
| `scroll-margin`             | Adds margin during scrolling.                         |
| `place-items`               | Aligns grid items easily.                             |
| `place-content`             | Aligns grid content.                                  |
| `isolation`                 | Controls stacking context.                            |
| `mix-blend-mode`            | Blends element with background.                       |
| `perspective`               | Adds 3D depth effect.                                 |
| `transform-style`           | Controls 3D child elements.                           |
| `backface-visibility`       | Hides back side during rotation.                      |
| `content`                   | Inserts generated content.                            |
| `counter-reset`             | Resets CSS counter.                                   |
| `counter-increment`         | Increases counter value.                              |
| `quotes`                    | Defines quotation marks style.                        |
| `tab-size`                  | Sets tab space width.                                 |
| `writing-mode`              | Changes text direction.                               |
| `direction`                 | Sets text direction (`ltr`, `rtl`).                   |
| `vertical-align`            | Aligns inline elements vertically.                    |
| `table-layout`              | Controls table layout method.                         |
| `caption-side`              | Positions table caption.                              |
| `empty-cells`               | Controls empty table cell borders.                    |
| `border-collapse`           | Merges table borders.                                 |
| `border-spacing`            | Sets spacing between table cells.                     |
---

# What I Learned Today

* Creating a static website.
* Using semantic HTML elements.
* Creating a navigation bar.
* Using separate HTML files.
* Loading external files using JavaScript `fetch()`.
* Starting CSS styling.
* Creating containers using Flexbox.
* Aligning content left, center, and right using CSS.
* Applying CSS properties for webpage design.




# HTML & CSS Interview Questions with Detailed Answers

# Basic Level Questions

## 1. What is HTML?

HTML stands for **HyperText Markup Language**. It is the standard language used to create webpages and structure content on the internet.

HTML uses different tags such as `<h1>`, `<p>`, `<img>`, and `<a>` to display text, images, links, tables, forms, and other webpage elements.

### Example:

```html
<!DOCTYPE html>
<html>
<head>
    <title>My Website</title>
</head>
<body>
    <h1>Welcome</h1>
    <p>This is a webpage.</p>
</body>
</html>
```

---

## 2. What is CSS?

CSS stands for **Cascading Style Sheets**. It is used to design and style HTML webpages.

CSS controls:

* Colors
* Fonts
* Layouts
* Spacing
* Animations
* Responsive design

### Example:

```css
h1{
    color: blue;
    text-align: center;
}
```

---

## 3. What is the purpose of semantic HTML elements?

Semantic elements clearly describe the meaning of content.

Examples:

* `<header>`
* `<nav>`
* `<section>`
* `<article>`
* `<footer>`

### Benefits:

* Improves readability
* Better SEO
* Helps screen readers
* Better webpage structure

---

## 4. What is the use of the `<header>` tag?

The `<header>` tag is used for the top section of a webpage.

It usually contains:

* Logo
* Website title
* Navigation menu
* Search bar

### Example:

```html
<header>
    <h1>Car Hubs</h1>
</header>
```

---

## 5. What is the use of the `<nav>` tag?

The `<nav>` tag is used to create navigation links.

### Example:

```html
<nav>
    <a href="#">Home</a>
    <a href="#">About</a>
</nav>
```

It helps users move between pages.

---

## 6. What is the purpose of the `<section>` tag?

The `<section>` tag divides content into different sections.

### Example:

```html
<section>
    <h2>Services</h2>
    <p>We provide car rentals.</p>
</section>
```

---

## 7. What is the role of the `<main>` element?

The `<main>` element contains the main content of the webpage.

There should be only one `<main>` tag per page.

### Example:

```html
<main>
    <h2>Main Content</h2>
</main>
```

---

## 8. Why is the `<footer>` tag used?

The `<footer>` tag is used for the bottom section of the webpage.

It usually contains:

* Copyright
* Contact information
* Social media links

### Example:

```html
<footer>
    <p>© 2026 Car Hubs</p>
</footer>
```

---

## 9. What is a static website?

A static website shows fixed content to users.

### Features:

* Built using HTML, CSS, and JavaScript
* No database required
* Fast loading
* Simple hosting

### Example:

Portfolio website, company website.

---

## 10. What is the purpose of the `<div>` tag?

The `<div>` tag is a container used to group elements together.

### Example:

```html
<div class="card">
    <h2>BMW</h2>
</div>
```

It helps in layout and styling.

---

## 11. What is the use of the `<link>` tag in HTML?

The `<link>` tag connects external files like CSS.

### Example:

```html
<link rel="stylesheet" href="style.css">
```

---

## 12. Why do we use external CSS files?

External CSS keeps design separate from HTML.

### Advantages:

* Reusable code
* Easy maintenance
* Faster editing
* Cleaner code

---

## 13. What is Flexbox in CSS?

Flexbox is a CSS layout system used to align elements easily.

### Example:

```css
.container{
    display: flex;
}
```

---

## 14. What does `display: flex` do?

It converts a container into a flex container.

Child elements become flexible items.

### Example:

```css
.container{
    display: flex;
}
```

---

## 15. What is the use of `justify-content`?

It aligns items horizontally.

### Example:

```css
justify-content: center;
```

### Values:

* center
* flex-start
* flex-end
* space-between
* space-around

---

## 16. What is the use of `align-items`?

It aligns items vertically.

### Example:

```css
align-items: center;
```

---

## 17. Difference between `margin` and `padding`.

| Margin                       | Padding                      |
| ---------------------------- | ---------------------------- |
| Space outside border         | Space inside border          |
| Creates gap between elements | Creates space inside element |

---

## 18. What is `box-sizing: border-box`?

It includes padding and border inside the element width.

### Example:

```css
*{
    box-sizing: border-box;
}
```

This makes layout calculations easier.

---

## 19. What is the purpose of JavaScript `fetch()`?

`fetch()` is used to load data or files asynchronously.

### Example:

```javascript
fetch('header.html')
```

It helps load external content dynamically.

---

## 20. Why do we use separate HTML files like `header.html`?

Separate files help create reusable components.

### Benefits:

* Better organization
* Reusable code
* Easier maintenance

---

# Moderate Level Questions

## 1. Explain semantic HTML with examples.

Semantic HTML uses meaningful tags that describe content.

### Examples:

```html
<header></header>
<nav></nav>
<section></section>
<footer></footer>
```

These tags improve SEO and accessibility.

---

## 2. What are the advantages of using semantic elements?

### Advantages:

* Better webpage structure
* Easier maintenance
* SEO improvement
* Accessibility support
* Cleaner code

---

## 3. How does a navigation bar work in a website?

A navigation bar contains links to different pages.

### Example:

```html
<nav>
    <a href="home.html">Home</a>
</nav>
```

Users click links to navigate.

---

## 4. Difference between inline, internal, and external CSS.

| Type         | Description                     |
| ------------ | ------------------------------- |
| Inline CSS   | Written inside HTML tag         |
| Internal CSS | Written inside `<style>`        |
| External CSS | Written in separate `.css` file |

---

## 5. Explain how Flexbox helps in webpage layout design.

Flexbox helps:

* Align items
* Create responsive layouts
* Distribute space evenly
* Center elements easily

---

## 6. Difference between `justify-content` and `align-items`.

| justify-content      | align-items        |
| -------------------- | ------------------ |
| Horizontal alignment | Vertical alignment |

---

## 7. How does `fetch()` work in JavaScript?

`fetch()` sends a request to get data/files.

### Example:

```javascript
fetch('data.json')
.then(response => response.json())
.then(data => console.log(data))
```

---

## 8. Why is reusable code important in web development?

Reusable code:

* Saves time
* Reduces repetition
* Improves maintainability
* Makes projects scalable

---

## 9. What is the purpose of `querySelector()` in JavaScript?

It selects HTML elements.

### Example:

```javascript
document.querySelector(".box")
```

---

## 10. Explain the working of this code:

```javascript
fetch('header.html')
```

This code requests the `header.html` file from the server.

The fetched content can then be displayed dynamically on the webpage.

---

## 11. What is the use of `.then()` in JavaScript?

`.then()` handles successful completion of promises.

### Example:

```javascript
fetch('data.json')
.then(response => response.json())
```

---

## 12. Why do we use `.catch()` in JavaScript?

`.catch()` handles errors.

### Example:

```javascript
.catch(error => console.log(error))
```

---

## 13. What is responsive web design?

Responsive design makes websites adjust to different screen sizes.

### Devices:

* Mobile
* Tablet
* Desktop

---

## 14. What are media queries in CSS?

Media queries apply CSS based on screen size.

### Example:

```css
@media(max-width:768px){
    body{
        background: lightblue;
    }
}
```

---

## 15. Difference between `id` and `class` in HTML.

| id          | class             |
| ----------- | ----------------- |
| Unique      | Reusable          |
| One element | Multiple elements |

---

## 16. What is the purpose of `z-index` in CSS?

`z-index` controls stacking order.

Higher value appears on top.

### Example:

```css
z-index: 10;
```

---

## 17. Explain the difference between `position: relative` and `position: absolute`.

| Relative                     | Absolute                 |
| ---------------------------- | ------------------------ |
| Moves from original position | Moves relative to parent |

---

## 18. What is the purpose of `transition` in CSS?

`transition` creates smooth animation effects.

### Example:

```css
transition: 0.5s;
```

---

## 19. What is the use of `animation` and `@keyframes`?

They create advanced animations.

### Example:

```css
@keyframes move{
    from{left:0;}
    to{left:100px;}
}
```

---

## 20. How does Flexbox align items left, center, and right?

Using:

```css
justify-content: flex-start;
justify-content: center;
justify-content: flex-end;
```

---

# Advanced Level Questions

## 1. Explain the complete workflow of loading external HTML files using JavaScript.

### Steps:

1. JavaScript sends request using `fetch()`
2. Server returns HTML file
3. Response converts into text
4. Content inserted using `innerHTML`

### Example:

```javascript
fetch('header.html')
.then(response => response.text())
.then(data => {
    document.getElementById("header").innerHTML = data;
})
```

---

## 2. What are the benefits of component-based design in web development?

### Benefits:

* Reusable components
* Easy updates
* Better organization
* Faster development

---

## 3. How does `fetch()` improve project scalability?

It allows:

* Dynamic loading
* API integration
* Modular design
* Better performance

---

## 4. Explain asynchronous programming in JavaScript.

Asynchronous programming allows tasks to run without blocking other operations.

### Example:

```javascript
fetch('data.json')
```

The webpage continues working while data loads.

---

## 5. Difference between synchronous and asynchronous code.

| Synchronous       | Asynchronous       |
| ----------------- | ------------------ |
| Runs line by line | Runs independently |
| Blocks execution  | Non-blocking       |

---

## 6. What is the DOM in JavaScript?

DOM stands for **Document Object Model**.

It represents HTML as objects.

JavaScript uses DOM to:

* Change text
* Add elements
* Remove elements

---

## 7. How does `innerHTML` work?

`innerHTML` inserts or changes HTML content.

### Example:

```javascript
element.innerHTML = "<h1>Hello</h1>";
```

---

## 8. What are the advantages and disadvantages of using `innerHTML`?

### Advantages:

* Easy to use
* Fast content updates

### Disadvantages:

* Security risks
* Replaces existing content

---

## 9. Explain the CSS Box Model.

The Box Model consists of:

1. Content
2. Padding
3. Border
4. Margin

\text{Total Width} = \text{Content} + \text{Padding} + \text{Border} + \text{Margin}

---

## 10. What is the difference between Flexbox and CSS Grid?

| Flexbox         | Grid             |
| --------------- | ---------------- |
| One-dimensional | Two-dimensional  |
| Row OR column   | Rows AND columns |

---

## 11. Explain how `display: flex` changes the behavior of child elements.

Child elements become flexible and align automatically.

They can:

* Resize
* Align
* Distribute space

---

## 12. What is the importance of semantic HTML in SEO?

Semantic HTML helps search engines understand webpage structure.

This improves:

* Search ranking
* Accessibility
* Content indexing

---

## 13. How does CSS improve user experience?

CSS improves:

* Design
* Layout
* Readability
* Animations
* Responsiveness

---

## 14. What is the purpose of `overflow` in CSS?

`overflow` controls extra content.

### Values:

* hidden
* scroll
* auto

---

## 15. Explain how `transform` and `transition` work together.

`transform` changes element shape or position.

`transition` makes changes smooth.

### Example:

```css
.box:hover{
    transform: scale(1.1);
    transition: 0.5s;
}
```

---

## 16. What is the role of media queries in responsive design?

Media queries adapt websites for different devices.

### Example:

```css
@media(max-width:600px)
```

---

## 17. How can animations improve website interactivity?

Animations:

* Attract users
* Improve user engagement
* Provide visual feedback
* Make UI modern

---

## 18. Explain the difference between `visibility: hidden` and `display: none`.

| visibility: hidden               | display: none              |
| -------------------------------- | -------------------------- |
| Element hidden but space remains | Element removed completely |

---

## 19. What are pseudo-elements and pseudo-classes in CSS?

### Pseudo-class:

Used for element states.

Example:

```css
button:hover
```

### Pseudo-element:

Used for styling specific parts.

Example:

```css
p::first-letter
```

---

## 20. How would you optimize a static website project for better performance?

### Methods:

* Compress images
* Minify CSS and JS
* Use external CSS
* Reduce unnecessary code
* Use caching
* Optimize animations
* Use semantic HTML
* Make website responsive

